"""End-user tools. Consumes /models projections and /scoring as black boxes —
never re-derives a projection or a point value inline.

v1: draft_tool.py (snake draft board with value-over-replacement).
Later: weekly_tool.py, the v2 injury/adjustment layer, a dashboard.
"""
